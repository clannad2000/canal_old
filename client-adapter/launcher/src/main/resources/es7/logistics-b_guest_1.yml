dataSourceKey: defaultDS
destination: example
groupId: g3
esMapping:
  _index: logistics-b_guest
  _type: _doc
#  _id: _id
  upsert: false
  pk: tel
  syncMode: simple
  specialFields:
    defaultVals:
      type: default
      properties:
        isRegistered: 1
  sql: "select
               tel,
               company as guestName,
               account_state as accountState,
               user_state as userState
        from s_user"
  #objFields:
    #brand: object
  #  etlCondition: "where a.c_time>='{0}'"
  commitBatch: 3000
