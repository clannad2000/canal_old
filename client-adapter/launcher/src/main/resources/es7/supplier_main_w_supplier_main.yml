dataSourceKey: defaultDS
destination: example
groupId: g2
esMapping:
  _index: supplier_main
  _type: _doc
  _id: supplierId
#  pk: supplierId
  syncMode: simple
  specialFields:
    brand:
      type: nested
      element: id,url,brandId,childName,carName,carId,groupId
  sql: "select supplier_id as supplierId,
               id,
               url,
               brand_id    as brandId,
               child_name  as childName,
               car_name    as carName,
               car_id      as carId,
               group_id    as groupId,
               concat(child_name,',',car_name) as fullName
        from w_supplier_main"
#  objFields:
#    brand: object
  #  etlCondition: "where a.c_time>='{0}'"
  commitBatch: 3000
